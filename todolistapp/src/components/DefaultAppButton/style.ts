import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  defaultAppButton: {
    backgroundColor: "#c5c3ad",
    alignItems: "center", // Alinha horizontalmente
    justifyContent: "center", // Alinha verticalmente
    height: 40,
    borderRadius: 50,
    marginVertical: 5
  },
});

export { styles };
