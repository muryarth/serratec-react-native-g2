import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  tabBarIconContainer: {
    alignItems: "center",
    gap: 3,
    height: 55,
    paddingTop: 8,
  },
  tabBarIconText: {
    fontSize: 14,
    fontWeight: "bold"
  },
});

export { styles };
