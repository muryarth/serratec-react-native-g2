import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  headerContainer: {
    alignItems: "center",
    marginTop: 24,
    marginBottom: 8,
  },

  headerText: {
    fontSize: 20,
    marginVertical: 6,
  },
});

export { styles };
