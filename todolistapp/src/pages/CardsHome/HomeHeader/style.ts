import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  headerContainer: {
    alignItems: "center",
    marginBottom: 16,
  },

  headerText: {
    fontSize: 20,
    marginVertical: 6,
  },
});

export { styles };
