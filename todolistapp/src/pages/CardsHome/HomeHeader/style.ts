import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  headerContainer: {
    alignItems: "center",
  },

  headerText: {
    fontSize: 20,
    marginVertical: 26,
  },
});

export { styles };
