import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  homeContainerListEmpty: {
    flex: 1,
    flexDirection: "row",
    alignItems: "center",
  },
});

export { styles };
